# 09 — Risk Matrix: generic risks + mitigations

Review pre-event; re-check at every gate. Add event-specific rows at the top.

| Risk | Mitigation | Trigger → action |
|---|---|---|
| **[DOMAIN] Hệ thống phán "vi phạm"/"sai" tuyệt đối** — luật có ngoại lệ/điều kiện không mô hình hóa hết trong 48h; kết luận sai = mất uy tín trước judge và rủi ro pháp lý | Taxonomy nhãn ĐÓNG: `dung / hieu_lam / can_kiem_chung` — không tồn tại nhãn "vi phạm"; mọi nhãn kèm "cần đối chiếu điểm X khoản Y Điều Z"; enum enforce trong P1 model + P9 rail | Ai đề xuất thêm nhãn phán xét → chỉ vào dòng này, từ chối; demo bị hỏi "sao không kết luận luôn?" → trả lời: thẩm quyền kết luận thuộc cán bộ, hệ thống chỉ đối chiếu |
| **[DOMAIN] LLM trích xuất sai điều khoản/mức phạt lúc ingest** — sai 1 con số là hỏng toàn bộ credibility | Ingest chạy 1 LẦN từ `data/nd174_trich.md` (đã đối chiếu docx gốc) → human verify từng node → FREEZE (commit JSON đã duyệt); engine không bao giờ gọi LLM | Phát hiện node sai sau freeze → sửa JSON tay + ghi collab log + chạy lại test P2, KHÔNG re-run LLM ingest |
| **[DOMAIN] Không scrape được Facebook/MXH thật** — Facebook chặn scrape/search tự do | Đã chốt: bộ bình luận mô phỏng 3 nhóm (spec trong `13_DOMAIN_BRIEF.md`), văn phong fanpage/forum; pipeline thiết kế để cắm nguồn thật sau | Judge hỏi nguồn dữ liệu → khai minh bạch "mô phỏng theo mẫu thảo luận công khai, giá trị nằm ở logic phân loại + KG, nguồn thật cắm vào P4 không đổi kiến trúc" |
| **[DOMAIN] Nhầm mức phạt cá nhân/tổ chức (bẫy Điều 4 k3)** — chính hệ thống mắc đúng hiểu lầm mà nó phải phát hiện | Mức cá nhân KHÔNG lưu cứng — luôn derive = 1/2 mức tổ chức qua hàm pure có test riêng (P2.1); UI luôn hiện cả 2 cột chủ thể | Test 1/2 đỏ hoặc UI hiện 1 mức không ghi chủ thể → block merge (gate G1/G2) |
| **[DOMAIN] Study case lộ danh tính người thật** — demo "quét vi phạm" trên tên thật = tự tạo rủi ro xúc phạm danh dự | Chỉ dùng vụ đã được cơ quan chức năng công bố chính thức; ẩn danh hóa trong UI ("cá nhân N.V.A, Quyết định số .../QĐ-XPHC"); P9 rail lọc PII | Ai paste tên thật/link profile vào data → PII rail chặn + xóa khỏi fixture ngay |
| **[DOMAIN] Judge hỏi "khác gì thuvienphapluat/luatvietnam?"** | Câu trả lời chuẩn bị sẵn (pitch §Q&A): họ là thư viện tra cứu tĩnh — ta là giám sát CHỦ ĐỘNG: feed → phân loại → hồ sơ đối tượng → diff cũ/mới tự động + tầng kiểm chứng bằng vụ thật | Câu hỏi xuất hiện → Pitcher trả lời theo script, demo mở hàng đợi ngay |
| **[DOMAIN] Prompt injection qua text bình luận** — comment là untrusted input đi vào LLM ở P4 | P9 rail: quét pattern lệnh ("ignore previous", "you are now", markers) → neutralize thành quoted data; eval gate có case injection | Injection lọt qua demo → đã có case eval chứng minh rail hoạt động, show test thay vì live |
| **[DOMAIN] Whitelist coverage gap → phạt oan** — "không tìm thấy nguồn" bị suy diễn thành "tin sai" trong khi corpus lệch crawl vài phút / tin địa phương / paywall (chính Test 1 ask.txt: SBV công bố 5 phút trước) | Nhãn nguồn `chua_tim_thay_nguon` là TRUNG TÍNH (chỉ đẩy ưu tiên xem xét); vắng bằng chứng ≠ bằng chứng vắng — enforce bằng P9 rail 5 + eval case chống-phạt-oan; corpus 48h là snapshot tĩnh khai minh bạch | Ai viết logic "không có nguồn → nghi vấn sai độ tin cậy cao" → chỉ vào dòng này + rail 5, sửa ngay; judge hỏi tin nóng → trả lời "corpus tĩnh cho demo, kiến trúc cắm crawler, và nhãn trung tính chính là để chống lỗi này" |
| **[DOMAIN] Pitch nói số % chính xác tự phong ("FP<1%")** — không có N đủ lớn đứng sau, judge biết thống kê sẽ hỏi và mất toàn bộ credibility | Chỉ nói confusion matrix trên eval set kèm N công khai + thiên vị thiết kế (thà lọt vào hàng đợi hơn gợi ý oan) + vụ kiểm chứng SCB khớp kết luận thật; script trả lời sẵn trong 13_DOMAIN_BRIEF §7 | Slide/pitch xuất hiện % không có N → Pitcher xóa trước rehearsal; bị hỏi trực tiếp → dùng script §7 |
| Problem statement doesn't fit any prep | `02` intake protocol; the core is domain-agnostic — **pivot the surface, not the core**; fallback tracks pre-ranked | Fit scores all <3 at H0 → Product Lead picks nearest archetype, narrows scope hard |
| Venue wifi blocks/throttles APIs | Test at hour 0 (checklists/HOUR_0); mobile hotspot backup; lightweight HTTPS providers; pre-ask organizers about hosting/MCP ports | First failed call → hotspot immediately, don't debug the venue network |
| API quota exhaustion mid-run | Multiple keys (primary+fallback rotation), free-tier mix, paid fallback with a cost cap; budget table in `03` filled pre-event | Quota error → key rotation is in P3; if all keys dry, halve N, keep CI honesty |
| Rebuild slower than planned | Milestone gates with the pre-agreed **cut list** (`05`); parallelism map keeps builders unblocked | Gate red → execute next cut within 10 min, log the decision |
| Live demo fails on stage | Video is the primary demo; live is the encore; pre-rendered static fallback page | Flake on stage → switch to fallback without commentary |
| "You prepared too much" challenge | `01` ethics line verbatim; all commits timestamped in-window; offer to regenerate any module live | Challenge raised → disclosure paragraph + live-regen offer |
| Scope creep after freeze | Charter rule: post-Gate-2 is cut-only; Product Lead sole cut authority | "Quick feature" proposed → answer is the rule, not a debate |
| Demo data looks weak | Schedule a second, bigger run at 50–65%; the honesty metrics (CIs, error counts) ARE a feature — pitch statistical honesty | Small N → show the CI-vs-N story instead of hiding it |
| Team burnout / hour-40 mistakes | Sleep shifts (min 4 h/night); gate meetings pinned to clock; blockers escalated at 20 min | Someone stuck+silent → anyone may call it, swap driver |
| Secrets leak in public repo | `.env` gitignored from P0; pre-submit secrets grep (checklists/PRE_SUBMIT) | Leak found → rotate key first, then scrub history |
| Submission portal failure | Submit at 95%, not 100%; all deliverables exported to local + drive backup | Portal down at submit → organizer escalation with timestamped backup in hand |
| {{TODO-EVENT: rủi ro riêng theo rules BTC — điền khi có thể lệ}} | {{TODO-EVENT}} | {{TODO-EVENT}} |
